import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  listContainer: {
    display: "flex",
    flexDirection: "row"
  },
  buttonContainer: {
    width: 300,
    marginTop: 23,
    padding: 7,
    height: "100%"
  }
});

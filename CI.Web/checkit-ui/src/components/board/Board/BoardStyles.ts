import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  container: {
    backgroundColor: "#fff",
    height: "700px",
  },
  listContainer: {
    display: "flex",
    flexDirection: "row",
  },
  buttonContainer: {
    width: 300,
    marginTop: 23,
    padding: 7,
    height: "100%",
  },
});
